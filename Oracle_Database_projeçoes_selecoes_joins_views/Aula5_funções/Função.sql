--Uma função é uma fórmula ou um algoritmo onde eu passo parâmetros, 
--e essa função retorna sempre um único valor, 
--baseado naqueles parâmetros que eu coloquei para essa função. 
SELECT <CAMPO>
FROM <TABELA>
WHERE <CONDIÇÃO>

--É uma função, passando como parâmetro valores constantes ou nome de campos.
SELECT F(CAMPO,...) 
FROM TABELA

--quatro grupos: 
--funções específicas para campos do tipo texto; 
--funções específicas para datas; 
--funções para serem aplicadas sobre números, ou seja, funções matemáticas; 
--Funções de conversão de dados, onde eu converto, por exemplo, uma data em um texto, ou um texto em um número e assim por diante. 
